# Apéritif

## NeoVim

Prepare:

```bash
cd NeoVim
chmod +x import.sh
chmod +x export.sh
```

Import configs:

```bash
APERITIF=/path/to/here ./import.sh
```

Export configs:

```bash
APERITIF=/path/to/here ./export.sh
```
